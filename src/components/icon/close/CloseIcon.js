import React from 'react'

import ClearIcon from '@material-ui/icons/ClearRounded'

/**
 * Wrapper for Clear/Close icon indicators.
 */
function CloseIcon(props) {
	return <ClearIcon {...props} />
}

export default CloseIcon
